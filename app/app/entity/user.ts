export interface User {
  // Unique user identifier from the API.
  id   : string;
  // Display name of the user from the API.
  name : string;
  // Optional email address from the API.
  mail?: string;
}
